version: '3.8'

services:
  app:
    image: webdevops/php-nginx:8.2-alpine
    container_name: nipnime-app
    restart: unless-stopped
    working_dir: /app
    volumes:
      - ./:/app
      - ./storage:/app/storage
      - ./public:/app/public
    ports:
      - "8085:80"
    environment:
      - WEB_DOCUMENT_ROOT=/app/public
      - PHP_MEMORY_LIMIT=2048M
      - PHP_UPLOAD_MAX_FILESIZE=2000M
      - PHP_POST_MAX_SIZE=2000M
      - NGINX_CLIENT_MAX_BODY_SIZE=2000m
    depends_on:
      - mysql
    networks:
      - nipnime-network

  queue:
    image: webdevops/php-nginx:8.2-alpine
    container_name: nipnime-queue
    restart: unless-stopped
    working_dir: /app
    volumes:
      - ./:/app
      - ./storage:/app/storage
    command: php artisan queue:work --queue=default --tries=3 --sleep=3 --timeout=90
    depends_on:
      - mysql
      - app
    environment:
      - WEB_DOCUMENT_ROOT=/app/public
      - PHP_MEMORY_LIMIT=2048M
      - PHP_UPLOAD_MAX_FILESIZE=2000M
      - PHP_POST_MAX_SIZE=2000M
      - NGINX_CLIENT_MAX_BODY_SIZE=2000m
    networks:
      - nipnime-network

  mysql:
    image: mysql:8.0
    container_name: nipnime-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: nipnime_secret_password
      MYSQL_DATABASE: web_anime
      MYSQL_USER: nipnime
      MYSQL_PASSWORD: nipnime_password
    volumes:
      - mysql_data:/var/lib/mysql
      - ./anime_db_backup.sql:/docker-entrypoint-initdb.d/init.sql
    ports:
      - "3307:3306"
    networks:
      - nipnime-network

  phpmyadmin:
    image: phpmyadmin:latest
    container_name: nipnime-phpmyadmin
    restart: unless-stopped
    environment:
      PMA_HOST: mysql
      PMA_PORT: 3306
      UPLOAD_LIMIT: 100M
    ports:
      - "8086:80"
    depends_on:
      - mysql
    networks:
      - nipnime-network

networks:
  nipnime-network:
    driver: bridge

volumes:
  mysql_data:
